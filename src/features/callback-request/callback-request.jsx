import styles from "./callback-request.module.css";

export const CallbackRequest = (props) => {
  return (
    <div className={styles.callbackRequest} {...props}>
      <p className={styles.todo}>CallbackRequest — TODO: свёрстать по макету</p>
    </div>
  );
};
