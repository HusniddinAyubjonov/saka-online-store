import styles from "./price-list-request.module.css";

export const PriceListRequest = (props) => {
  return (
    <div className={styles.priceListRequest} {...props}>
      <p className={styles.todo}>PriceListRequest — TODO: свёрстать по макету</p>
    </div>
  );
};
