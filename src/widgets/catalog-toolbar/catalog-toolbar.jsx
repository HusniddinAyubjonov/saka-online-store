import styles from "./catalog-toolbar.module.css";

export const CatalogToolbar = (props) => {
  return (
    <div className={styles.catalogToolbar} {...props}>
      <p className={styles.todo}>CatalogToolbar — TODO: свёрстать по макету</p>
    </div>
  );
};
