import styles from "./delivery-methods.module.css";

export const DeliveryMethods = (props) => {
  return (
    <div className={styles.deliveryMethods} {...props}>
      <p className={styles.todo}>DeliveryMethods — TODO: свёрстать по макету</p>
    </div>
  );
};
