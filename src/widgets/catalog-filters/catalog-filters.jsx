import styles from "./catalog-filters.module.css";

export const CatalogFilters = (props) => {
  return (
    <div className={styles.catalogFilters} {...props}>
      <p className={styles.todo}>CatalogFilters — TODO: свёрстать по макету</p>
    </div>
  );
};
