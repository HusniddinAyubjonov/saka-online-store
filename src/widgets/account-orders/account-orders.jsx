import styles from "./account-orders.module.css";

export const AccountOrders = (props) => {
  return (
    <div className={styles.accountOrders} {...props}>
      <p className={styles.todo}>AccountOrders — TODO: свёрстать по макету</p>
    </div>
  );
};
