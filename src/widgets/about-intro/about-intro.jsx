import styles from "./about-intro.module.css";

export const AboutIntro = (props) => {
  return (
    <div className={styles.aboutIntro} {...props}>
      <p className={styles.todo}>AboutIntro — TODO: свёрстать по макету</p>
    </div>
  );
};
