import styles from "./certificates.module.css";

export const Certificates = (props) => {
  return (
    <div className={styles.certificates} {...props}>
      <p className={styles.todo}>Certificates — TODO: свёрстать по макету</p>
    </div>
  );
};
