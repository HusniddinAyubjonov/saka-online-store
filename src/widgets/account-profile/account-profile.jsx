import styles from "./account-profile.module.css";

export const AccountProfile = (props) => {
  return (
    <div className={styles.accountProfile} {...props}>
      <p className={styles.todo}>AccountProfile — TODO: свёрстать по макету</p>
    </div>
  );
};
