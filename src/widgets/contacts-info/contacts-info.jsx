import styles from "./contacts-info.module.css";

export const ContactsInfo = (props) => {
  return (
    <div className={styles.contactsInfo} {...props}>
      <p className={styles.todo}>ContactsInfo — TODO: свёрстать по макету</p>
    </div>
  );
};
