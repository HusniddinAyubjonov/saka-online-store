import styles from "./palette-catalog.module.css";

export const PaletteCatalog = (props) => {
  return (
    <div className={styles.paletteCatalog} {...props}>
      <p className={styles.todo}>PaletteCatalog — TODO: свёрстать по макету</p>
    </div>
  );
};
