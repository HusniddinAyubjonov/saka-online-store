import styles from "./contacts-map.module.css";

export const ContactsMap = (props) => {
  return (
    <div className={styles.contactsMap} {...props}>
      <p className={styles.todo}>ContactsMap — TODO: свёрстать по макету</p>
    </div>
  );
};
