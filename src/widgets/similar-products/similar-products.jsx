import styles from "./similar-products.module.css";

export const SimilarProducts = (props) => {
  return (
    <div className={styles.similarProducts} {...props}>
      <p className={styles.todo}>SimilarProducts — TODO: свёрстать по макету</p>
    </div>
  );
};
