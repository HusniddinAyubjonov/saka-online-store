import styles from "./cart-summary.module.css";

export const CartSummary = (props) => {
  return (
    <div className={styles.cartSummary} {...props}>
      <p className={styles.todo}>CartSummary — TODO: свёрстать по макету</p>
    </div>
  );
};
