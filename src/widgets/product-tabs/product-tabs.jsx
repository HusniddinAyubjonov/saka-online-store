import styles from "./product-tabs.module.css";

export const ProductTabs = (props) => {
  return (
    <div className={styles.productTabs} {...props}>
      <p className={styles.todo}>ProductTabs — TODO: свёрстать по макету</p>
    </div>
  );
};
