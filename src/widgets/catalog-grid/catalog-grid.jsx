import styles from "./catalog-grid.module.css";

export const CatalogGrid = (props) => {
  return (
    <div className={styles.catalogGrid} {...props}>
      <p className={styles.todo}>CatalogGrid — TODO: свёрстать по макету</p>
    </div>
  );
};
