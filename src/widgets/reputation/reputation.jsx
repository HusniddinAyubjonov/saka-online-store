import styles from "./reputation.module.css";

export const Reputation = (props) => {
  return (
    <div className={styles.reputation} {...props}>
      <p className={styles.todo}>Reputation — TODO: свёрстать по макету</p>
    </div>
  );
};
