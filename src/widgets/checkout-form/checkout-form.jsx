import styles from "./checkout-form.module.css";

export const CheckoutForm = (props) => {
  return (
    <div className={styles.checkoutForm} {...props}>
      <p className={styles.todo}>CheckoutForm — TODO: свёрстать по макету</p>
    </div>
  );
};
