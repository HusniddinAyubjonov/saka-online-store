import styles from "./certificate-card.module.css";

export const CertificateCard = (props) => {
  return (
    <div className={styles.certificateCard} {...props}>
      <p className={styles.todo}>CertificateCard — TODO: свёрстать по макету</p>
    </div>
  );
};
