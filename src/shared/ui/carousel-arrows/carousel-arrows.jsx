import styles from "./carousel-arrows.module.css";

export const CarouselArrows = (props) => {
  return (
    <div className={styles.carouselArrows} {...props}>
      <p className={styles.todo}>CarouselArrows — TODO: свёрстать по макету</p>
    </div>
  );
};
