import styles from "./slider-dots.module.css";

export const SliderDots = (props) => {
  return (
    <div className={styles.sliderDots} {...props}>
      <p className={styles.todo}>SliderDots — TODO: свёрстать по макету</p>
    </div>
  );
};
